{
  "name": "pew",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf build",
    "build": "yarn run clean && tsc",
    "postbuild": "copyfiles -u 1 public/**/* build",
    "build-dev": "yarn run clean && tsc -w",
    "postbuild-dev": "copyfiles -u 1 public/**/* build",
    "prestart": "yarn run build-dev",
    "start": "browser-sync build -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.1.1",
    "rimraf": "^3.0.0",
    "typescript": "^3.6.4"
  }
}